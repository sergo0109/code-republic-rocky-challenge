#include <iostream>
#include <vector>
using namespace std;
int comdiv(int &x,int &y){
    if((x==0)||(y==0)){
        return 1;
    };
    if(x<0){
        x*=-1;
    };
      if(y<0){
        y*=-1;
    };
    if(x==y){
        return x;
    };
    if(y<x){
        x+=y;
        y=x-y;
        x-=y;
    };
    if(y%x==0){
        return x;
    };
    int z=1;
    while((x%2==0)&&(y%2==0)){
        x/=2;
        y/=2;
        z*=2;
    };
    bool check=false;
    vector<int> simnum;
    simnum.push_back(2);
    simnum.push_back(3);
        for(int j=3;j<x;j+=2){
            for(int i=1;i<simnum.size();++i){
                if(j%simnum[i]==0){
                i=simnum.size();
                check=false;
                } else{
                check=true;
                };
            };
            if(check==true){
                simnum.push_back(j);
                while((x%j==0)&&(y%j==0)){
                    x/=j;
                    y/=j;
                    z*=j;
                };
            };
        };
        return z;
    }
int main()
{
    int x;
    cin>>x;
    int y;
    cin>>y;
    cout<<comdiv(x,y);
    return 0;
}
